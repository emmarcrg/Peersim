{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Launch Simulator",
            "request": "launch",
            "mainClass": "peersim.Simulator",
            "args": ["config_file.cfg"],
            "cwd": "${workspaceFolder}"
        }
    ]
}